<%= form_with(model: deal_category) do |form| %>
  <% if deal_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deal_category.errors.count, "error") %> prohibited this deal_category from being saved:</h2>

      <ul>
        <% deal_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :deal_id, style: "display: block" %>
    <%= form.text_field :deal_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
